coverage:
  status:
    project:
      default: false
      main:
        paths:
          - "src/evaluating_rewards/"
      tests:
        # Should not have dead code in our tests
        target: 100%
        paths:
          - "tests/"
    patch:
      default: false
      main:
        paths:
          - "src/evaluating_rewards/"
      tests:
        target: 100%
        paths:
          - "tests/"
